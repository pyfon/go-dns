- Fix the fact that a CNAME query for recursive.example.com returns the FQDN recursive1. This could be the cause of the bug when doing a normal query for recursive.example.com
- This should mean the you can test recursion with:

zone example.com.
recursive   CNAME   recursive1
recursive1  CNAME   recursive

dig recursive.example.com

- Fix TXT record issues.
- Test out all records.
